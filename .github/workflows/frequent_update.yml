name: Frequent Update

on:
  schedule:
    # Запуск каждые 2 часа
    - cron: "0 */2 * * *"
  # Позволяет запустить скрипт вручную кнопкой в GitHub
  workflow_dispatch:

jobs:
  update-files:
    runs-on: ubuntu-latest
    # Права на запись необходимы для авто-обновления бинарника xray
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Добавлен pysocks для поддержки socks5 в requests
          pip install --no-cache-dir requests PyGithub pysocks

      - name: Run logic
        env:
          MY_TOKEN: ${{ secrets.MY_TOKEN }}
        run: |
          # Переходим в папку со скриптом и запускаем
          cd source && python main.py
